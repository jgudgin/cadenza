"""Optional, project-agnostic agents that ship with cadenza itself.

Nothing in `cadenza.orchestrator` or `cadenza.registry` depends on this
package - it exists purely as a home for reusable agents (like
`self_maintain`) that a project can opt into.
"""
